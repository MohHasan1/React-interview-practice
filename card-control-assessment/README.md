# Card Control Dashboard assessment

See [INSTRUCTIONS.md](./INSTRUCTIONS.md) for the exercise requirements and UI contract. The project includes a local mock API, Vitest/React Testing Library checks, and ESLint.

Run `npm install`, then `npm run dev`. Other checks: `npm test`, `npm run build`, and `npm run lint`.
