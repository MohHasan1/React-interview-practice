# Transaction Review Console

Read [INSTRUCTIONS.md](./INSTRUCTIONS.md) before starting the 75-minute practice assessment.

Run `npm install` and `npm run dev` to start. Required tests run with `npm test`; optional bonus tests run separately with `npm run test:bonus`.
